# 2.0.0 · 2026-09-10

- 客户端改为账号密码登录，自动加密保存并复用登录状态。
- 客户端设置电脑名称和完整预设，网页自动展示同账号电脑并负责提问与查看回答；同名电脑始终按独立 ID 区分。
- 合并配置为公共和个人预设，支持完整编辑、停用、权限隔离和问答配置版本保存。
- 每个账号默认最多保存 5 台电脑，可通过 MAX_DEVICES_PER_USER 调整；删除电脑撤销登录和共享访问并释放名额。
- 使用统一版本文件，对登录、实时连接和截图上传执行版本一致性校验。
- 破坏性升级：检测旧数据库后清空本项目数据表并重建，不迁移；当前版本数据库保留。
- 调整网页和客户端文案，移除凭证操作入口，完善手机导航与无预设提示。
- 预设配置恢复提示词、请求格式、模型端点、API 密钥与标准参数名称；管理员界面保留审计日志和操作标识，用户登录页避免无关技术说明。

# 更新记录

## 2026-09-07

- 补齐账号禁用、凭证撤销、密码变更及浏览器实时会话失效处理。
- 解绑和重置连接码时同步取消请求、撤销授权、使待审批许可失效；加入连接码碰撞处理和重置重试确认。
- 为请求状态变更加入单进程互斥保护，避免重复上传、取消被覆盖及旧连接更新新会话。
- 增加截图超时与服务重启恢复，将运行状态初始化移出通用应用工厂，避免清理命令误改在线状态。
- 验证图片格式与解码，分离图片大小和上传表单大小限制，统一接口时间的时区信息。
- 按模型协议筛选参数，检测流式错误和不完整输出；Gemini 密钥改为请求头传递。
- 修复客户端取消后立即提问、重连线程隔离、待确认连接码保存、界面事件队列和等比例图片压缩。
- 修复网页注册与凭证创建反馈、多行模型编辑、模型重新启用、请求草稿保留、手机端退出和 WebSocket 关闭处理。
- 新增账号与设备生命周期、模型错误流、真实本地 HTTP/WebSocket 通路、客户端并发与 DPAPI 等回归测试。
- 提供本地服务启动入口及可指定 Python 解释器的 Windows 单文件打包脚本。

本轮未执行真实供应商调用与目标服务器容器部署；该部分需要实际模型配置和部署环境。
